package com.user.management.model.enumeration;

public enum ProvisioningEventType {
    USER_CREATED,
    USER_UPDATED,
    USER_DELETED
//    USER_LOGIN (What to send and what will be persisted in the user service?)
}
